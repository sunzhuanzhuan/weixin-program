<!--detail.wxml-->
<!--detail.wxml-->
<view>
    <scroll-view scroll-y style="height:100%;position: absolute;" bindscroll="recordUserscroll">
    <!--播放视频的位置-->
      <view wx:if="{{videoSource.length>0}}"  style="heigh:200rpx;margin:0 24rpx;{{videoTop}}" >
          <view>
              <txv-video vid="{{videoId}}" playerid="video" autoplay="{{true}}" muted="{{isMuted}}"></txv-video>
          </view>
          <view wx:if="{{videoSource.length==1}}" style=" height:80rpx;margin: 30rpx 0;">
             <view  bindtap="handleIsMuted" style="width:100rpx; height: 70rpx;line-height:70rpx;float: right;text-align: center;margin-right: 30rpx;margin-top: -2rpx" >
                 <image style="width:40rpx; height: 40rpx;" src="{{isMuted?'/images/mute.png':'/images/noMute.png'}}"></image>
             </view>
              <text style="color: #E6E6E6;display:inline-block;height:40rpx;margin-right: 50rpx;float: right;"> | </text>
          </view>
          <view wx:else style="text-align: center;">
              <view wx:for="{{videoSource}}" data-videoId="{{index}}" bindtap="handleVideoTap" class="{{selectedCricle == index?'borderCircle':'borderNum'}}">
                  <text style="font-size: 24rpx">视频{{index+1}}</text>
              </view>
              <view  bindtap="handleIsMuted" style="width:100rpx; height: 70rpx;line-height:70rpx;float: right;text-align: center;margin-right: 30rpx;margin-top: 46rpx" >
                  <image style="width:40rpx; height: 40rpx;" src="{{isMuted?'/images/mute.png':'/images/noMute.png'}}"></image>
              </view>
              <text class="line"> </text>
          </view>

      </view>
        <rich-text class="_root" nodes="{{nodes}}"></rich-text>
    </scroll-view>
    <!--悬浮部分-->
    <view class="fixed {{isShow?'show':''}} state1">
        <view class="floatShadow"></view>
        <view style="display: flex;flex-direction: row;background: #ffffff;height: 94rpx;">
            <view  wx:if="{{isEyes}}"  style="flex:3;visibility: hidden;">123</view>
            <button  wx:else class="leftBtn" data-item="know" bindgetuserinfo="handleAuthor" open-type="{{type}}"
                    style="flex:3;">
                <!--hidden="{{isEyes}}"-->
                <image class="eyeBtn" src="../../images/eye.png"></image>
                <text class="readLetter">让朋友知道我在读</text>
            </button>
            <button catchtap="handleLike" data-item="like" bindgetuserinfo="handleAuthor" open-type="{{type}}"
                    style="text-align:center;opacity: 1;background:#ffffff;flex:0.5;position: relative;z-index: 2;{{isIphoneX?'height:194rpx':'height:134rpx'}}">

                <image class="imgBtn" style="position: relative;z-index: 1;"
                       src="{{isLike?('../../images/likeAfter.png'):('../../images/likeBefore.png')}}"></image>

            </button>
            <button open-type="{{type1}}" data-item="share" bindgetuserinfo="handleAuthor"
                    style="text-align:center;border-color: #ffffff;opacity: 1;background:#ffffff;flex:0.5;position: relative;z-index: 2;{{isIphoneX?'height:194rpx':'height:134rpx'}}">
                <image src="{{shareButton}}" class="imgBtn" style="position: relative;z-index: 1;"></image>
                <!-- <image class="eyeBtn" src="../../images/eye.png"></image> -->
            </button>
            <!--<button open-type="{{type}}"  data-item="poster" bindgetuserinfo="handleAuthor" catchtap="handlePoster"-->
                    <!--style="border-color: #ffffff;opacity: 1;background:#ffffff;flex:1;position: relative;z-index: 2;{{isIphoneX?'height:194rpx':'height:134rpx'}}">-->
                <!--生成海报-->
            <!--</button>-->
        </view>
        <view wx:if="{{isIphoneX}}" class="bol"></view>
        <view wx:else></view>
    </view>
</view>

<!--悬浮回到首页-->
<image src="/images/goHome.png" class="{{isShare?'goHomeStyle':'goHomeStyle1'}} state1" bindtap="handleBack"></image>
